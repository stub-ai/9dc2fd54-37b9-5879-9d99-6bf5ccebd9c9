; AutoHotkey script

SetTimer, Check, 300000 ; 5 minutes in milliseconds

MouseGetPos, X, Y
return

Check:
MouseGetPos, NewX, NewY
if (NewX = X and NewY = Y)
{
    ; Mouse hasn't moved, perform your action here
    MsgBox, Mouse hasn't moved for 5 minutes.
}
else
{
    ; Mouse has moved, update the position
    X := NewX
    Y := NewY
}
return